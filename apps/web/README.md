# @snf/web

The 404-snf Nuxt frontend. It uses Nuxt UI for the interface and Nuxt i18n for
localized routes and copy. BLE protocol definitions live in
`app/utils/protocol.ts`.

```bash
pnpm --filter @snf/web dev
pnpm --filter @snf/web build
pnpm --filter @snf/web typecheck
```
